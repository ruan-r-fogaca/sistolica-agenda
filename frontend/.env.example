# Só é necessário em produção (ex: Vercel), apontando para o backend publicado.
# Em desenvolvimento local, deixe vazio: o Vite já faz proxy de /api para o backend.
VITE_API_URL=https://sua-api-backend.onrender.com/api
